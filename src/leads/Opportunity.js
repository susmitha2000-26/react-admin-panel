import React from 'react';
import { Box, Typography } from '@mui/material';

const Opportunity = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>Opportunity</Typography>
      <Typography>This is the Opportunity page content.</Typography>
    </Box>
  );
};

export default Opportunity;
